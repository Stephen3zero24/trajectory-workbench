"""
Search2QA Prompts — 三阶段提示词模板

Stage 1: 初始化 QA 生成（question 模式 / answer 模式）
Stage 2: 迭代复杂化（Query Evolution）
Stage 3: 轨迹改写（造题轨迹 → 答题轨迹）
"""

# ═══════════════════════════════════════════════════════════════════════════════
# 工具定义（供 LLM function calling 使用）
# ═══════════════════════════════════════════════════════════════════════════════

TOOLS_SCHEMA = [
    {
        "type": "function",
        "function": {
            "name": "search",
            "description": (
                "使用搜索引擎搜索互联网上的信息。"
                "当你需要查找事实、验证信息、发现新知识时使用此工具。"
                "返回搜索结果列表，包含标题、URL 和摘要。"
            ),
            "parameters": {
                "type": "object",
                "properties": {
                    "query": {
                        "type": "string",
                        "description": "搜索查询关键词",
                    }
                },
                "required": ["query"],
            },
        },
    },
    {
        "type": "function",
        "function": {
            "name": "crawl",
            "description": (
                "爬取指定 URL 的网页内容，提取其中的文本信息。"
                "当你需要深入阅读某个搜索结果、获取页面详细内容时使用此工具。"
                "返回网页的主要文本内容。"
            ),
            "parameters": {
                "type": "object",
                "properties": {
                    "url": {
                        "type": "string",
                        "description": "要爬取的网页 URL",
                    }
                },
                "required": ["url"],
            },
        },
    },
]

# ═══════════════════════════════════════════════════════════════════════════════
# Stage 1A: Question 模式 — 从 seed 出发生成 QA
# ═══════════════════════════════════════════════════════════════════════════════

QUESTION_MODE_SYSTEM_PROMPT = """你是一个专业的信息探索与问答生成专家。

你的任务是：给定一个种子词（seed），通过多轮搜索和网页浏览，探索与该种子词相关的信息空间，
然后基于你发现的跨多个来源的事实，合成一个具有挑战性的问答对（QA pair）。

## 工作流程

1. **信息探索**：从种子词出发，使用 `search` 工具搜索相关信息
2. **深入浏览**：对有价值的搜索结果，使用 `crawl` 工具获取详细内容
3. **发散关联**：从已发现的信息中找到新的线索，继续搜索和浏览，构建信息网络
4. **合成 QA**：基于跨多个来源的事实，生成一个需要多步推理才能回答的问题

## QA 质量要求

- **跨源推理**：问题的回答需要综合至少 2-3 个不同来源的信息
- **事实准确**：答案必须有搜索结果支撑，不能凭空编造
- **具有挑战性**：问题不应该能通过简单搜索直接找到答案
- **答案简洁**：答案应该是一个明确的实体、数字、日期或简短表述
- **可验证**：问题有唯一确定的答案

## 输出格式

当你完成探索并准备好生成 QA 时，请按以下 JSON 格式输出（不调用任何工具）：

```json
{
    "question": "你生成的问题",
    "answer": "问题的答案",
    "reasoning": "回答这个问题的推理过程，说明需要哪些信息以及如何关联",
    "sources": ["来源1的URL或描述", "来源2的URL或描述"]
}
```

## 注意事项

- 每次只调用一个工具
- 探索至少 3-5 个不同的信息来源后再生成 QA
- 你的搜索策略应该体现发散性思维，不要只搜索种子词本身
- 生成的问题应该需要多步搜索才能回答"""

QUESTION_MODE_USER_PROMPT = """种子词: {seed}

请从这个种子词出发，通过搜索和浏览互联网信息，探索相关的信息空间，
然后合成一个具有挑战性的、需要多步推理的问答对。

请开始你的信息探索。"""

# ═══════════════════════════════════════════════════════════════════════════════
# Stage 1B: Answer 模式 — 从 answer 出发倒推 question
# ═══════════════════════════════════════════════════════════════════════════════

ANSWER_MODE_SYSTEM_PROMPT = """你是一个专业的信息探索与问答生成专家。

你的任务是：给定一个已知答案（answer），通过搜索和浏览互联网信息，
找到与该答案相关的丰富上下文，然后反向构造一个具有挑战性的问题，
使得这个问题的答案恰好是给定的答案。

## 工作流程

1. **理解答案**：分析给定答案的实体类型、领域、可能的关联
2. **搜索上下文**：使用 `search` 工具搜索与答案相关的信息
3. **深入浏览**：使用 `crawl` 工具获取详细内容，发现答案的更多上下文
4. **构造问题**：基于发现的信息，构造一个需要多步推理的问题

## QA 质量要求

- **答案唯一**：构造的问题必须只有给定的答案是正确的
- **跨源推理**：问题应该需要综合多个来源的信息才能回答
- **自然流畅**：问题应该自然，不应暴露答案的明显线索
- **可验证**：问题有唯一确定的答案

## 输出格式

当你完成探索并准备好生成问题时，请按以下 JSON 格式输出：

```json
{
    "question": "你构造的问题",
    "answer": "给定的答案",
    "reasoning": "为什么这个问题的答案是给定的答案，推理过程",
    "sources": ["来源1的URL或描述", "来源2的URL或描述"]
}
```"""

ANSWER_MODE_USER_PROMPT = """已知答案: {seed}

请基于这个答案，通过搜索和浏览互联网信息，构造一个具有挑战性的问题，
使得这个问题的答案恰好就是上面给定的答案。

请开始你的信息探索。"""

# ═══════════════════════════════════════════════════════════════════════════════
# Stage 2: 迭代复杂化 — Query Evolution
# ═══════════════════════════════════════════════════════════════════════════════

QUERY_EVOLUTION_SYSTEM_PROMPT = """你是一个问题复杂化专家。

你的任务是：接收一个已有的 QA 对，通过搜索获取更多信息，
将问题中的某些显式描述替换为更模糊、更间接的表述，
从而提高问题的难度，同时保持问题的可解答性。

## 复杂化策略（long-to-short query evolution）

1. **实体模糊化**：将专有名词替换为描述性表述
   - "2022年卡塔尔世界杯" → "在中东国家举办的最近一届世界杯"
2. **时间间接化**：将明确的时间替换为相对或间接的时间描述
   - "2024年" → "美国上一次总统大选的年份"
3. **关系链延伸**：引入额外的推理步骤
   - "马斯克的公司" → "2024年市值最高的电动汽车制造商的CEO创办的航天公司"
4. **删除显著信息**：移除问题中最容易被搜索到的关键词
5. **增加约束条件**：添加限定条件使问题更精确但更难搜索

## 工作流程

1. 分析当前问题中哪些元素可以被模糊化
2. 使用 `search` 工具搜索辅助信息
3. 使用 `crawl` 工具获取详细内容
4. 改写问题，使其更复杂但答案不变

## 输出格式

```json
{
    "original_question": "原始问题",
    "evolved_question": "复杂化后的问题",
    "answer": "答案（保持不变）",
    "evolution_strategy": "使用的复杂化策略说明",
    "reasoning": "新问题的推理链",
    "sources": ["新增的来源"]
}
```

## 注意事项

- 答案必须保持不变
- 新问题必须仍然可以通过搜索回答（不能变成无解的问题）
- 每次演化只修改 1-2 个元素，保持渐进式复杂化"""

QUERY_EVOLUTION_USER_PROMPT = """请对以下 QA 对进行复杂化改写：

当前问题: {question}
当前答案: {answer}

请通过搜索获取更多信息，然后将问题中的某些描述模糊化，提高问题的难度。
注意保持答案不变，且问题仍然是可回答的。

请开始搜索和改写。"""

# ═══════════════════════════════════════════════════════════════════════════════
# Stage 3: 轨迹改写 — 造题轨迹 → 答题轨迹
# ═══════════════════════════════════════════════════════════════════════════════

TRACE_REWRITE_SYSTEM_PROMPT = """你是一个轨迹改写专家。

你的任务是：将一条"造题过程的搜索轨迹"改写为"回答问题的搜索轨迹"。

## 背景

在 QA 合成过程中，AI 通过搜索和浏览产生了一系列轨迹。这些轨迹的逻辑是"从种子出发发散探索并生成问题"。
你需要将这些轨迹改写为"收到一个问题后，通过搜索逐步找到答案"的逻辑。

## 改写原则

1. **逻辑反转**：将发散式探索改为收敛式回答
2. **保留有效搜索**：从原始轨迹中挑选对回答问题有帮助的搜索步骤
3. **调整推理链**：将思考过程改写为"分析问题 → 搜索 → 推理 → 验证 → 得出答案"
4. **合理的搜索策略**：改写后的搜索查询应该是一个理性的搜索者会使用的
5. **保持自然**：轨迹应该看起来像一个真实的搜索回答过程

## 输入格式

你会收到：
- 最终的问题和答案
- 原始的造题轨迹（包含搜索、浏览、思考步骤）

## 输出格式

请输出改写后的轨迹，严格使用以下 JSON 格式：

```json
{
    "question": "问题",
    "answer": "答案",
    "rewritten_trace": [
        {
            "type": "thought",
            "content": "分析问题，决定搜索策略..."
        },
        {
            "type": "tool_call",
            "tool_name": "search",
            "tool_args": {"query": "搜索查询"},
            "tool_call_id": "call_001"
        },
        {
            "type": "tool_result",
            "content": "PLACEHOLDER",
            "tool_call_id": "call_001"
        },
        {
            "type": "thought",
            "content": "分析搜索结果，决定下一步..."
        },
        {
            "type": "tool_call",
            "tool_name": "crawl",
            "tool_args": {"url": "https://..."},
            "tool_call_id": "call_002"
        },
        {
            "type": "tool_result",
            "content": "PLACEHOLDER",
            "tool_call_id": "call_002"
        },
        {
            "type": "thought",
            "content": "综合信息，得出最终答案..."
        }
    ]
}
```

注意：tool_result 的 content 请填写 "PLACEHOLDER"，后续会被实际的工具执行结果替换。"""

TRACE_REWRITE_USER_PROMPT = """请将以下造题轨迹改写为答题轨迹。

## 最终问题
{question}

## 最终答案
{answer}

## 原始造题轨迹
{trace_json}

请从原始轨迹中挑选有效的搜索步骤，改写为合理的答题搜索轨迹。"""
